pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
               echo 'Compiling the java source code'
                bat 'mvn compile'
                }
             }

        stage('Test') {
            steps {
                echo 'Running the compiled java code.'
                bat 'mvn test -Dtest=CountryService'

                bat 'mvn test -Dtest=MembershipTypeService'
                bat 'mvn test -Dtest=OrganizationService'
                bat 'mvn test -Dtest=PermissionService'
                bat 'mvn test -Dtest=RoleService'
                bat 'mvn test -Dtest=UserService'
                bat 'mvn test -Dtest=UserStatusService'
                }
             }

        stage('Reports') {
            steps {
                script {
                echo  'JUnit reporting the test results'
                junit(
                      allowEmptyResults: true,
                      testResults: '**/target/surefire-reports/*.xml'
                     )
                  }
                script {
                echo  'Postman reporting the test results'
                bat 'C:/Users/Baran/AppData/Roaming/npm/newman run C:/Users/Baran/Desktop/postmancoll.json'
                  }
                }
             }
           }
        }