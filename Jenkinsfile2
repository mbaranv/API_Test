pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo 'Compiling the java source code'
                javac -cp UserService.java
            }
        }
        stage('run') {
            steps {
                echo 'Running the compiled java code.'
                java -cp UserService
            }
        }
    }
}